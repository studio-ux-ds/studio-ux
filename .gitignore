# Studio UX — projeto de documentação (fundação). / documentation project (foundation).
# Nesta fase não há build; ignoramos apenas artefatos de SO e editor.
# In this phase there is no build; we ignore only OS and editor artifacts.

# Sistema operacional / OS
.DS_Store
Thumbs.db
desktop.ini

# Editores / Editors
.vscode/
.idea/
*.swp
*~

# Temporários / Temporary
*.tmp
*.log
.fuse_hidden*
.fuse_hidden*/

# Futuro (fases de implementação) / Future (implementation phases)
node_modules/
dist/
build/
.cache/

# Empacotamento / Packaging
dist-packs/
*.tgz
.npmrc
package-lock.json

# Projetos gerados pelo Project Generator (saída, não fazem parte do framework)
dist-projects/
